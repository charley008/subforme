# v1.3.7

## 主要更新

- 优化手机端用户页布局，改为卡片式展示并提供完整可见的移动导航。
- 新增配置备份与恢复，恢复备份后会自动重启并在启动时应用数据。
- 后台登录改为 30 天持久会话，关闭浏览器后无需频繁重新登录。
- 更新默认直连、默认代理和 iOS 模板。
- 前端发布产物改为固定文件名 `assets/index.js` 与 `assets/index.css`。

# v1.3.6

## 主要更新

- 新增 iOS / 移动端订阅模板，可通过 `type=ios` 获取独立模板配置。
- 用户页分享链接支持选择默认订阅或 iOS 订阅，并直接复制对应链接。

# v1.3.5

## 主要更新

- 用户页节点配置改为弹窗式“节点设置”，更适合节点数量较多的场景。
- 新增用户级分组模式覆盖：每个用户都可以为每个分组单独选择 `select`、`url-test`、`fallback`、`load-balance`。
- `fallback` 模式新增顺序调整能力，可以直接在“分组设置”里控制节点或分组引用的优先级。
- 代理分组页移除全局“类型”编辑，分组模式统一由用户页的“分组设置”决定。

## 兼容说明

- 旧的分组类型配置仍然会被保留用于兼容已有数据。
- 当用户把分组模式切换为 `url-test`、`fallback` 或 `load-balance` 时，系统会自动补齐默认探测 URL 和间隔，避免生成不完整配置。

# v1.3.4

## 主要更新

- 流量统计切换为基于 3x-ui `clients/list` 的 client 级流量采集，更适配单用户多 inbound 模型。
- 新增服务器级流量调度配置：支持按分钟自动刷新面板流量，按月自动清零，并可单独设置日期、时间与时区。
- 面板流量重置改为调用 `resetAllTraffics`，与每月统一清零的使用方式保持一致。
- 优化概览页流量刷新性能：多台面板并发拉取，单台面板超时缩短，离线面板不会长时间拖慢整体刷新。
- 移除概览页手动重置面板流量按钮，避免与新的月度清零策略重复。

# v1.3.2

## 主要更新

- 概览页新增节点总流量统计。
- 修复概览页中同一节点在不同流量图表里的颜色不一致问题。

# v1.3.1

## 主要更新

- 新增深色模式，并优化模板编辑、用户管理、设置页在深色模式下的显示效果。
- 优化主面板导入统计，只有用户资料实际变化时才计入更新。
- 清理旧版配置接口，减少历史逻辑对当前数据库模式的干扰。
- 修复模板在线保存后的换行格式问题。

# v1.3.0

## 主要更新

- 重做 3x-ui 多服务器同步逻辑，支持新增、更新、关联入站和删除同步。
- 概览页新增流量刷新和按服务器重置用户流量功能。
- 优化 Provider 地址生成，适配反代和二级路径部署。

# v1.2.0

## 主要更新

- 用户、节点、分组和第三方 Provider 以 SQLite 为主要数据源。
- 第三方 Provider 自动维护对应代理分组，并在删除时清理残留配置。
- 代理分组管理改为保存即生效。

# v1.1.1

## 主要更新

- 新增第三方 Provider 转换和定时刷新。
- 后台关键操作输出到控制台日志，便于 Docker 部署排查。
- 优化第三方 Provider 删除、刷新和订阅生成逻辑。

# v1.1.0

## 主要更新

- 引入本地 SQLite 数据库。
- 新增多 3x-ui 面板管理和同步能力。
- 新增实时流量查看。
- 新增服务器管理、节点绑定和用户节点选择。
